# FVSC — Эволюция проекта

Хронология ключевых идей, решений и поворотов. Не техлог (для этого есть CHANGELOG.md), а *интеллектуальная история*: как менялось понимание задачи и подход к её решению.

---

## 2026-06-15 — Первый честный live-тест онбординга

**Поворот:** Rein потребовал пройти онбординг «как чужой человек» — бэкап cache и data.json, перезагрузка плагина с нуля. До этого MVP считался собранным на основании 125/125 + 11/11 зелёных тестов. Live-тест вскрыл, что **ни один из этих тестов не покрывал реальный путь нового пользователя**, и автодетект Python никогда не работал ни на одной машине — пользователи прошлых сессий просто вписывали путь вручную, что и маскировало баг.

**Что сделано:** Пять корневых багов, каждый из которых лежал в коде с момента написания соответствующего слоя:

1. **`testPython` формула версии** — `sys.version_info[0]*10+[1]` для Py3.13 даёт `43`, проверка `v >= 310` всегда ложна. Любая версия Python отбрасывалась как «слишком старая». Фикс: `*100+`.
2. **Obsidian не наследует bash PATH** — `python` через PATH не находится даже когда CPython установлен. Фикс: прямой системный поиск `%LOCALAPPDATA%\Programs\Python\Python3*\python.exe`, Program Files, py launcher. Аналог для macOS — задача следующей сессии.
3. **Backend race на double-build** — `bootstrap_running=True` ставился внутри генератора SSE, ПОСЛЕ возврата StreamingResponse. Двойной клик → два worker'а на один cache. Фикс: атомарный claim в эндпоинте.
4. **`/viz/status` не lazy-load cache** — главный виновник «карта не работает». Status смотрел `_state["space"]`, не вызывая `_get_space()`. Cache на диске → `space_loaded=false` → view рендерил CTA «Построить карту» → пользователь запускал build_from_vault на 95с каждый раз вместо использования существующего cache. Фикс: lazy-load в `viz_status()`.
5. **View self-dedup race** — первая логика `peers[0] !== ours` сохраняла первый дубль. Симптом: две копии view в одном pane. Фикс: симметричный detach всех других peers.

Плюс пакет UX-фиксов: singleton BootstrapModal, дизейбл кнопки билда, 409→passive polling, `scheduleBootstrapCheck` с ретраем.

**Почему именно так:** Любой UX-баг где «через раз» — это race условие, не магия. Любой шаг где пользователь видит «карта не построена» при наличии cache на диске — это баг lazy-load. И главное: тестовая зелёная зона (unit + smoke) **не покрывает путь нового пользователя**, потому что они тестируют API в зрелом состоянии, а не cold-start. Live-цикл «бэкап → reload → пройти онбординг до карты» обязателен перед заявлением «MVP готов».

**Что не сделано (приоритет следующей сессии):** Ollama auto-management. Rein зафиксировал чётко: «подразумевается что это само будет при наличии ollama её запускать и выбирать загруженную модель или предлагать выбрать из списка». Сейчас плагин показывает «Запусти Ollama» — это failed UX того же типа что был с venv path. Решение по аналогии с paths.ts: `ollama.ts` модуль с `detectOllama()`, `ensureOllamaRunning()`, model picker.

**Артефакты для interpretation lens:** `service/tests/test_interpretation.py` (pytest golden), `service/tests/interpret_cli.py` (интерактив), `service/tests/interpretation_cases.json` (стартовый кейс «хлам» + шаблон). Harness готов, прогон на Rein'овой карте — следующая сессия.

---

## 2026-06-13 — Тонкий онбординг как MVP-граница

**Поворот:** Накопился перекос — research-прототип развивался вглубь (provenance, silent_pool, live watcher), но «работает только у автора». Любой новый пользователь упирался в `503 «Run vault_sync first»`, обязательные поля Settings с путём к python.exe в venv'е, `503 «Ollama daemon not running»` без объяснения. Эмоциональный сигнал Rein'а: «устал расширять систему когда mvp не собран». Зафиксировали MVP как «новый человек открыл плагин и через ~2 мин видит свою карту» — не PyInstaller bundle, не нативный TS-граф, не CM6-подсветка, а именно этот тонкий онбординг-слой.

**Что сделано:** Четыре слоя поверх ядра:

1. **Backend** — `POST /viz/build_from_vault` через `asyncio.to_thread + queue.Queue`. Стримит 9 пар start/end progress events из синхронной `vault_sync.run()` без блокировки event loop. `_get_space()` возвращает `(None,None)` при отсутствии cache. `/viz/ask` без Ollama отдаёт SSE error вместо 503. `/viz/status.bootstrap_running` для UI-state.
2. **Plugin autodetect** — `src/paths.ts`: `detectPython` (bundle → venv → PATH) и `detectRepo` (7 кандидатов). `settings.ts` с RU-подсказками «Найдено: PATH [Использовать]». Учли Windows-venv-в-bin (git-bash layout).
3. **Plugin UX** — `src/bootstrap.ts`: `BootstrapModal` с прогресс-баром, SSE-чтение через `fetch().body.getReader()`. `view.ts` с empty-state CTA «Построить карту» когда space_loaded=false. Оранжевая плашка `.fvsc-ollama-hint` когда Ollama down. RU-error messages в `backend.ts` + `onConfigError` callback.
4. **Docs** — `INSTALL_RU.md` (7 шагов без техтерминов) + ссылка в README.

**Почему именно так:** Любой шаг с техтермином (venv, uvicorn, FastAPI) = failed UX по жёсткому критерию `feedback_mass_adoption.md`. Любая ошибка должна вести к понятному действию пользователя, не к 503. Bootstrap должен запускаться автоматически при отсутствии данных. Эти три принципа определили дизайн каждого слоя.

**Критичный технический подводный камень:** Долгая (95с) синхронная `vault_sync.run()` запускается в `asyncio.to_thread` (default thread pool), прогресс льётся через **thread-safe `queue.Queue`** (asyncio.Queue не подошла бы — её put_nowait не safe across threads), async generator опрашивает очередь каждые 100мс через `get_nowait`. Без этого паттерна FastAPI event loop блокировался бы на всю длину билда, плагин терял health-pings, status-bar мерцал. Backpressure через `put(timeout=1.0)` — потеря тика безопаснее зависания worker'а.

**Что осталось вне MVP** (этапы старого roadmap'а, перенесено): silent_pool в Антураже, CM6 `[[locate:...]]`, нативный TS-граф, PyInstaller bundle. Они полезные, но не блокеры «новый человек видит свою карту» — поэтому вытеснены за границу MVP.

---

## 2026-06-06 (вечер) — Provenance, silent_pool и живая карта

**Поворот:** До сегодня FVSC говорил «у тебя есть концепт X» и не мог сказать
«откуда у тебя X». Каждый Judgment помечался литералом `source_text="[vault]"`.
Карта была зеркалом смысла, но не зеркалом *генезиса* смысла. Для cognitive
alignment — главной миссии — этого мало: рефлексия требует знать «откуда».

**Что сделано:**
- `core/provenance.py` — per-file pass через тот же tokenizer что и парсер.
  Каждый концепт обретает реальное распределение по `.md` файлам vault'а.
- `density_core.load_from_semantic_input(provenance=...)` — один Judgment →
  N Judgment'ов с `modality * fraction` и `source_text = реальный путь.md`.
  Total mass на концепт сохраняется. ρ остаётся идентичной (encoder зависит
  от subject/verb/object — детерминирован).
- Endpoint `/viz/concepts/{term}/sources` → топ-N заметок с весами.
- UI: dblclick на узле графа → панель с `obsidian://open` ссылками.

**Подводный камень (зафиксирован в коде):** encoded vector в
`encode_judgment_for_subject` зависит от `j.intensity` (`v = v_obj + intensity*v_verb`).
Первая версия делила intensity на N — каждый из N Judgment'ов получал свой
нормализованный вектор, ρ размывалась, query_contains возвращал мизер. Фикс:
делим только `modality` (массу), `intensity` остаётся равным исходному значению.

**Концептуальный сдвиг — silent_pool.** Предложение Rein'а: что человек
сказал один раз для самого человека может быть критически важным, но
окружающие не могут это понять, если он говорит вскользь. Раньше FVSC через
`min_freq=5` буквально *выбрасывал* такие токены. Теперь — сохраняем
параллельной структурой без density-matrix.

- `SemanticSpace.silent_pool = {token: {freq, sources: {path: count}}}`.
- НЕ участвует в density-matrix / query_contains / compare_maps / retrieval.
- На vault Rein'а: 51,365 silent токенов, 29,024 произнесены однажды.
- Endpoint `/viz/silent`, UI fallback в drill-down с badge «🔇 silent · N×».

**Почему это правильно архитектурно:** стенография включает silent. Мы
записываем всё что было сказано, но не выдумываем структуру там где данных
нет (одно упоминание = нет co-occurrence). Это снимает противоречие между
«статистически значимое» (нужно для понимания между людьми) и «лично
значимое» (нужно для понимания себя). Без отбрасывания — оба слоя
доступны Антуражу.

**Living map.** Plugin-side `VaultWatcher` слушает изменения заметок,
debounce 1.5с, POST в `/viz/file_ingest`. Backend через `purge_source` +
`ingest_one_file` обновляет ρ инкрементально. Trade-off: глобальные
provenance fractions не пересчитываются — drift возможен через сотни
правок, лечится full rebuild.

**Сопутствующий сдвиг — Obsidian plugin.** Антураж переехал из браузера
в нативную вкладку Obsidian. TypeScript-плагин (`obsidian-plugin/`) сам
поднимает Python-бэкенд, передаёт vault_path через env var, рендерит
карту + чат внутри Obsidian, открывает заметки в split-view, кладёт
кэш в vault. cytoscape.js + cose layout заменили vis-network — узнаваемая
obsidian-эстетика с hover-focus и тонкими линиями.

**Что осталось:** silent_pool в Антураж system prompt'е (он должен уметь
ссылаться на silent в ответах), CM6-подсветка слов в открытой заметке
(маркер `[[locate:путь#"фраза"]]`), нативный TS-граф без iframe,
PyInstaller-bundle для mass adoption.

---

## 2026-06-06 (утро) — Антураж: LLM как драйвер визуализации, а не собеседник

**Поворот:** Чат как отдельный текстовый интерфейс — слабая интеграция. Сильная — когда LLM управляет визуализацией в реальном времени, синхронно со своим ответом подсвечивает узлы и связи карты.

**Ключевое архитектурное решение — единый адресный синтаксис.** LLM использует wikilink-подобные маркеры `[[concept:важно]]`, `[[edge:A->B]]`, `[[note:...]]`, `[[judgment:term#N]]`, `[[word:...]]` прямо в прозе. Парсер на стриме вытаскивает их и шлёт highlight-эвенты в браузер. Альтернатива (two-shot JSON) была отвергнута — медленно, ломает стрим.

**Почему это важно концептуально:** Визуализация перестаёт быть статичным артефактом, который пользователь сам осваивает. Она становится со-инструментом мышления: LLM указывает, что важно посмотреть, человек видит это в пространстве карты, движется глазами/курсором, формулирует следующий вопрос. Это режим *совместного исследования карты*, не Q&A.

**Реализовано:** MVP-1 — уровень концептов, подсветка узлов и рёбер. End-to-end протестировано: Ollama qwen2.5:14b → парсер → SSE → vis-network.

**Сопутствующий бонус — vault стал каноническим клиентом.** `core/vault_sync.py` делает один проход: vault → SemanticSpace → концепт-заметки `.md` в `_fvsc_concepts/` → HTML-карта в корне vault'а. Wikilinks превращают семантический граф в нативный граф-вью Obsidian. Интеграция стала двусторонней: текст Obsidian кормит FVSC, FVSC возвращает структуру обратно.

**Что отложено:** drill-down внутрь концепта (Components, заметки-источники), правильный provenance в Judgment, кластеры. Roadmap зафиксирован в `memory/antourage_vision.md`.

---

## 2026-03 (ранний) — Зарождение: контейнеры и стенография

**Исходная идея:** Значение слова для конкретного человека = то, что он помещает внутрь через свои формулировки. Не словарное определение, а персональное содержимое.

**Ключевые решения:**
- Модель контейнера: понятие = контейнер + содержимое других контейнеров
- Стенографический принцип: записывать только то, что следует из слов, не интерпретировать
- Суждение (S→V→O + свойства) как единица записи
- Глагол — одновременно связка и понятие-контейнер

**Теоретический фундамент:** Wittgenstein (значение = употребление), Frege (sense/reference), Kelly (личные конструкты), Neo-Davidsonian event semantics.

**Почему именно так:** Конфликты и непонимание между людьми — вопрос определений. Люди используют одни слова, но вкладывают разный смысл. Система делает эту разницу видимой.

---

## 2026-03 (середина) — Формализация: density matrices

**Поворот:** От наивных списков содержимого к операторам плотности из квантовой информатики.

**Почему density matrices, а не граф?**
- Polysemy = mixed state (von Neumann entropy)
- Asymmetric containment через Tr(ρ_A·ρ_B)/Tr(ρ_A)
- Eigendecomposition → грани смысла без предобучения
- Каждый компонент = rank-1 проектор с provenance (обратимость к исходному суждению)

**Реализовано:** `density_core.py` — Judgment, Component, Concept, SemanticSpace. Рекурсивное углубление ρ_deep.

**Осознание:** Density matrices — не "квантовая семантика" в физическом смысле. Это *инженерное решение*: data structure с полезными свойствами (спектральное разложение, асимметричное вхождение, энтропия как мера полисемии).

---

## v0.1 — Плоский экстрактор (baseline)

**Метод:** Итерация по токенам → VERB → nsubj/obj из прямых потомков.
**Результат:** 1026 суждений. "Человек" = хаб (44 как субъект). Отрицание плоское, модальность игнорируется, координация теряет второй элемент.
**Вывод:** Плоское извлечение непригодно — теряет структуру языка.

---

## v0.2 — Рекурсивный экстрактор

**Поворот:** Переход от плоского к рекурсивному обходу дерева зависимостей.

**Что появилось:**
- ExtractionContext: negation scope, modality, quantifiers, conditionals
- Neg-raising vs non-neg-raising глаголы
- Условные конструкции (если→то) с condition_id
- Координация через декартово произведение

**Результат:** 1013 суждений (−1.3%). Количество примерно то же, но *качество* принципиально выше: отрицание корректное, модальность дифференцированная.

---

## v0.3 — Контекстный классификатор

**Поворот:** Классификация *до* извлечения. Не всё, что человек говорит, определяет его смыслы.

**Ключевое осознание:** "Этот человек вчера пришёл" — это эпизод, не определение. "Человек смертен" — это убеждение. Нужно разделять ДО записи.

**Два уровня классификации:**
1. Clause: GENERIC / HABITUAL / EPISODIC
2. NP: CONCEPTUAL / SELF / INTERLOCUTOR / GENERIC / REFERENTIAL / QUOTE

**Результат:** 877 суждений (−13.4%). "Человек" перестал быть хабом. Обобщённое "ты" теперь извлекается.

**Сигналы:** Аспект (НСВ/СВ) — ключевой для русского, ILP/SLP предикаты, детерминеры, темпоральные маркеры.

---

## v0.4 — Тезаурус, аномалии, [self], визуализация

**Добавлено:**
- [self] routing: identity/relation/action → разные веса в контейнер [self]
- Тезаурусный слой: ConceptNet (132K) + RuWordNet (95K) как base layer (modality=0.3–0.4)
- Детекция аномалий: anomaly_score = 1 − Tr(|v⟩⟨v|·ρ_norm)
- Нормализация чатового текста (~120 сокращений)
- Интерактивная HTML-карта (vis.js) с рекурсивной навигацией внутрь понятий
- Три карты: по отдельности для каждого участника + общая

**Запущено:** Генерация карт из полного корпуса (51756 сообщений).

---

## 2026-03-25 — Whitepaper до ~2064 строк

**Что зафиксировано:**
- Полный 9-шаговый пайплайн с формулами
- Рекурсивная алгебраическая архитектура (XIV) — изоморфизм с TreeRNN/GNN
- Все лексиконы контекстного классификатора (Приложение A)
- 9 оригинальных вкладов формализованы

---

## 2026-03-27 — Критический пересмотр оснований

**Триггер:** Желание критически оценить фундамент — корректно ли выбрана стратегия картографирования персональных смыслов.

**Четыре фундаментальных вопроса и решения:**

### F1. Стенографический принцип — иллюзия чистоты
**Осознание:** Чистая стенография уже нарушена — context_classifier совершает инференции (обобщённо-личное "ты", GENERIC по аспекту). Это не дефект, а неизбежность.

**Решение:** Слоистая модель интерпретационной уверенности:
- L0 — прямое извлечение из синтаксиса (жёсткое ядро)
- L1 — лингвистические инференции (отменяемые)
- L2 — синергия с Антуражем (отменяемые, маркированные)

Каждое суждение маркируется `interpretation_layer`, `defeasible`, `inference_chain`.

**Опора:** Grice (1975) said/implicated; Default Logic (Reiter 1980); AGM belief revision (1985).

### F2. Проблема заземления
**Осознание:** Базовые вектора из spaCy = коллективные, не персональные. Для малого корпуса карта отражает коллективный язык, а не индивида.

**Решение:** "Персональный прогрев" — PCA по контекстам при >50 суждениях для понятия. Перспектива: гиперболическое вложение (Nickel & Kiela 2017).

**Опора:** Saussure (langue vs parole); Derrida (différance).

### F3. Система как модель, не зеркало
**Осознание:** "Могу объяснить → могу формализовать" — верно, но с оговорками. Dreyfus: embodied knowledge; Polanyi: tacit knowledge. FVSC записывает *нарратив* (как человек объясняет себя), а не *механизм*.

**Решение:** Позиционировать как полезную модель (Box 1976). Добавить `extraction_confidence`.

### F4. Забывание со следами
**Осознание:** Накопление без забывания не отражает динамику смыслов. Но ничего не должно исчезать бесследно.

**Решение:** Степенной decay `(1+Δt/τ)^{-d}` вместо экспоненциального. Архивация слабых компонентов (provenance сохранён). Консолидация через повторное подтверждение.

**Опора:** ACT-R (Anderson 1993); MINERVA 2 (Hintzman 1988); CLS (McClelland et al. 1995).

**Результат:** Whitepaper расширен до ~2250 строк. Новые разделы XVII (Эпистемологические основания) и XVIII (Временная динамика). 21 новый академический источник. Обновлены "Актуальные вопросы" (F1–F4 + T1–T9).

---

## 2026-03-27 — Имплементация: v0.5

**Реализованы три задачи в `density_core.py`:**

### F1 → код: слоистая модель в Judgment
Добавлены поля `interpretation_layer` (0/1/2), `defeasible`, `inference_chain`, `extraction_confidence`. Новый метод `Concept.rho_layer(max_layer)` строит ρ только из компонентов нужного слоя. Теперь можно "отключить" L1/L2 и увидеть чистое ядро.

### F3 → код: степенной decay + консолидация + архивация
- `_recompute_rho()` теперь применяет степенной decay: `w(t) = w₀ · activation_count · (1+Δt/τ)^{-0.5}`
- Компоненты ниже порога `ε·max(w)` → `archived=True` (в ρ не участвуют, provenance сохранён)
- `add_component()` теперь проверяет косинусное сходство: если >0.85, не добавляет дубликат, а увеличивает `activation_count` существующего компонента
- `reactivate()` восстанавливает из архива

### T6 → код: сравнение карт
`SemanticSpace.compare_maps(space_a, space_b)` — для каждого общего понятия считает `Tr(ρ_A^norm · ρ_B^norm)`. Возвращает divergent/aligned/global_similarity.

**Тесты:** test_poc.py проходит. Ручные тесты decay, консолидации, сравнения карт, фильтрации по слоям — все ок.

### F2 → код: персональный прогрев
`get_term_vector()` теперь трёхуровневый: если у понятия >50 компонентов — строит персональный seed через PCA (SVD по накопленным контекстам) вместо spaCy. Метод `_personal_basis()` возвращает первую главную компоненту. spaCy используется только как инициализация до набора персональных данных.

### T5 → код: relation-dependent transform для recursive deepening
`_relation_transform(ρ, relation)` — similarity transform `R_r·ρ·R_r†`, где R_r — ортогональная матрица, детерминированная от глагола связи. Сохраняет trace и eigenvalues, но поворачивает грани смысла в зависимости от отношения. `_dominant_relation(A, B)` находит самый частый глагол, связывающий A→B в суждениях. Recursive deepening теперь учитывает *через что* A содержит B.

### T10 → код: интерактивный канал обратной связи (FeedbackEngine)
Новый модуль `core/feedback.py`. Система перестала быть пассивной — появился третий канал входа (наряду с текстовым анализом и тезаурусом): интерактивная калибровка через диалог с Антуражем.

**FeedbackEngine** анализирует SemanticSpace и генерирует приоритизированные мягкие вопросы:
- Аномалии (prio=0.9): "Это что-то новое, или я неправильно понял контекст?"
- Противоречия (0.85): "Тут есть нюанс. Что ближе к правде?"
- Defeasible L1/L2 (0.6): "Я предположил X. Это похоже на правду?"
- Архив (0.5): "Раньше было важно X. Это по-прежнему так?"
- Контрасты по Kelly (0.3): "Чем X отличается от Y для тебя?"
- Milestones (0.2): "Я начинаю понимать, что для тебя значит X"

Ответы обновляют карту: подтверждение → consolidation, отклонение → архивация, промоция L1→L0.

В Judgment добавлены: `confirmation_status` ("unreviewed"/"confirmed"/"rejected"/"contextualized"), `context_tags` (контекстуальные убеждения).

**Принципы дизайна:** никогда не обвинять, всегда можно пропустить, непрямые exploratory вопросы, gamified milestones.

---

## 2026-04-07 — Сегментация предложений: v0.7.3

**Проблема:** Telegram-сообщения приходят без пунктуации. `live_test.py` мержит последовательные сообщения в блоки (45s окно), spaCy получает слитный текст без точек → `doc.sents` считает весь блок одним предложением → дерево зависимостей ломается → extraction теряет суждения из второго/третьего предложения. Тихий false negative, невидимый в evaluation (GOLD_STANDARD тестирует только одиночные чистые предложения).

**Решение:** WTSplit/SaT (Segment any Text, ACL 2024) — нейросетевой punctuation-agnostic сегментатор как L1 препроцессор.

**Архитектурный выбор:**
- Сегментация предложений лингвистически универсальна — это не про персональную семантику, а про общую грамматику. Поэтому нейросеть на этом уровне приемлема (в отличие от extraction, где важен персональный контекст).
- SaT классифицирует позиции как "граница/не граница" — не генерирует и не меняет текст.
- Детерминистичен при инференсе (нет рандомности).
- Защита от разрыва модальных конвертов: сегменты, начинающиеся с "что/чтобы/как/который", склеиваются обратно с предыдущим.
- Graceful degradation: если wtpsplit не установлен, fallback на split по newline (старое поведение).

**Принцип минимальной инвазии:** tree_extractor.py и text_normalizer.py не изменены. SaT работает upstream — подаёт spaCy уже отдельные предложения. `segment_and_flatten()` реплицирует timestamps для сохранения alignment contract.

**Временное решение:** Будет заменено при реализации T11 (морфологическое ядро), где pymorphy2 + падежная грамматика определят границы клауз детерминистически.

**Новый пайплайн:**
```
raw blocks (merged 45s) → segment_sentences(SaT) → per-sentence: normalize_text() → spaCy → _walk() → Judgment[]
```

---

## 2026-05-27/28 — Полный переход на агностичное ядро

**Триггер:** spaCy не установлен, весь старый пайплайн (`tree_extractor`,
`context_classifier`, `live_test`, `interactive_map`, etc.) не запускается.
Параллельный агностичный стек (`text_parser_agnostic`, `semantic_input`,
`basis_vectors`) — рабочий. Решение: убить spaCy-ветку, сделать агностику
основной.

### Что произошло

**1. Удаление spaCy-зависимых модулей** — 10 файлов, ~4000 строк удалены
из main. Полностью сохранено в git history до коммита `530bc3d`.

**2. Бридж парсер ↔ density_core**
`SemanticSpace.load_from_semantic_input(si_dict)` — раньше агностичный стек
жил параллельно с density_core. Теперь они связаны: co-occurrence веса
конвертируются в синтетические Judgments (`interpretation_layer=1`) и
проходят через полный пайплайн density_core (decay, consolidation, anomaly).

**3. RichJudgment — все 6 слоёв** (из файла `переход на язык-агностик систему.txt`)
Аддитивное расширение `Judgment` с дефолтами:
- L1 уже был — добавлены `modality_type`, `negation_scope`
- L2 фреймовый — `frame_name`, `semantic_roles`, `role_intensity`
- L3 полисемический — `facet_id`, `polysemy_degree`, `sense_vector`
- L4 телесный — `perceptual_modalities`, `perceptual_features`, `emotion_tags`
- L5 социальный — `context_metadata`, `historical_variant`
- L6 рефлексивный — `user_marked_facet`, `user_confidence`

Слоты есть, каналы заполнения L2-L6 будут построены постепенно.

**4. Математические инварианты — 125/125**
`test_invariants.py`: 15 групп проверок, всё проходит. Свойства ρ (симметрия,
PSD), нормировка, асимметрия containment, границы энтропии (S≥0, S=0 для pure
state, S≤log d), purity, симметрия trace inner product, консолидация, слои
интерпретации, decay монотонность, бридж semantic_input, RichJudgment
round-trip, backward compat.

Это первый раз когда у математического ядра есть тесты — раньше regressions
ловились только через eval на чистом тексте.

**5. Eval framework — переписан**
Старый `evaluation.py` требовал spaCy и измерял S→V→O extraction (которого
больше нет). Новый: agnostic pipeline, gold set с полным графом концептов
(глагол как контейнер тоже). Метрика — наличие containment-пары в
semantic_input.

**Baseline зафиксирован: Recall=100%, Precision=66%, F1=79%** на 16
предложениях, 46 пар. 24 FP — sibling-пары (силы↔терпения через общий
глагол), приемлемый шум для накопительной системы.

**6. Тезаурус-приор — bonus-only**
Архитектурный урок: penalty за "оба термина известны но пара не в тезаурусе"
убивает абстрактные пары (свобода→ответственность нет в ConceptNet). Поэтому
только bonus за подтверждённые пары. ConceptNet RU — 95K пар, загрузка 0.2s.

### Архитектурные принципы (зафиксировано)

**Каскад семантики, не backprop.** Скелет (тезаурус) → адаптация
(co-occurrence из персонального потребления) → шлифовка (FeedbackEngine).
Физика памяти (ACT-R): decay, consolidation, archive. НЕ градиентный спуск.

**Ось L4/L5 — биология vs культура.** L4 (perceptual, emotion) универсален
— баseline grounding общий для всех людей. L5 (social, ideology) культурно
вариативен — источник реальных конфликтов смыслов. Это операционально важно
для `compare_maps`: расхождение по L5 при сходстве L4 = "одинаково чувствуем,
по-разному осмысляем".

### Что важно для будущего

Recall 100% на baseline означает что чисто co-occurrence из text находит
**все** правильные связи. Precision 66% — что много ложных. Это правильное
распределение для накопительной системы: лучше принять шум и дать
consolidation+decay+feedback его выгрызть со временем, чем фильтровать на
входе и потерять реальные связи.

**Следующая фаза:** тестирование на больших объёмах (eval_sentences_200,
а затем целые книги). Главная гипотеза — на масштабе шум усредняется,
правильные связи усиливаются через повторяемость.

---

## 2026-05-31 — Экзокортекс и эмпирический ответ про тезаурус

**Поворот:** От synthetic gold set к реальному пользовательскому корпусу.
Telegram-экспорты и Obsidian-структура собраны в единый источник и прогнаны
через FVSC pipeline. Это первая встреча системы с настоящим объёмом
персональной семантики (вне публичных бенчмарков).

### Что произошло

**1. Сборка ingest-pipeline.** 14 источников с разными регистрами
конвертированы в MD-файлы с YAML-frontmatter. Регистр источника автоматически
наследуется как L5 `context_metadata` — впервые слой 5 заполняется не
вручную, а из источника. Это первое практическое применение L5.

**2. Диагностика на представительном корпусе (~3500 сообщений).** Три
предсказания архитектуры подтверждены на реальных, не синтетических данных:
- **Асимметрия containment работает** на множестве пар с Δ ~0.9-0.95
  при B→A ≈ 0. Это первая эмпирическая проверка
  `Tr(ρ_A·ρ_B)/Tr(ρ_A) ≠ Tr(ρ_B·ρ_A)/Tr(ρ_B)` не на синтетике.
- **Полисемия как энтропия фон Неймана** даёт операциональных кандидатов
  на L6: высокочастотные концепты с poly>1.8, 4-5 фасетов автоматически
  выделяются как точки расхождения смыслов. Особое: класс высокочастотных
  аффективных маркеров оказывается полноценными полисемичными концептами
  с 5 фасетами — обнаружение без априорной разметки.
- **Sibling-FP виден как класс ошибок** — случайные co-occurrence дают
  высокий Δ, подтверждая проблему #6 NEXT_SESSION количественно.

**3. Полный прогон vault (606 файлов, 3.5M символов) + ConceptNet.**
Эта точка — момент эмпирического ответа на главный вопрос проекта.

**4. Численный ответ: «достаточно ли тезауруса?»**
Соотношение покрытия тезаурусом:
- 80% top концептов есть в ConceptNet (узлы)
- 1.2% пар подтверждены ConceptNet (рёбра)
- → Тезаурус знает **словарный запас**, не **персональные связи**

Это переводит ранее философское утверждение "тезаурус даёт скелет, корпус —
мясо" в численное доказательство в соотношении 80% vs 1.2%. Если бы мы
строили систему только из тезауруса, получили бы лишь 1.2% реально
существующих связей пользователя. Корпус даёт 98.8% рёбер личной семантики.

**5. Принцип «тезаурус валидирует, корпус извлекает».** Стратегия
bonus-only закреплена не как осторожность, а как архитектурная
необходимость: любая стратегия "обогащения через тезаурус" уничтожила бы
персональную семантику. Около 20% top концептов отсутствуют в ConceptNet —
разговорные формы и аффективные маркеры — by design не покрытые KB.

### Что важно для будущего

Виден новый методологический вопрос: **что можно прочитать о пользователе
из ρ-графа в отрыве от исходных текстов?** Если ρ — это адекватная
репрезентация смысловой реальности (Леонтьев), то её должно быть
достаточно для построения профиля без чтения исходника. Это становится
новым приоритетным направлением.

Также карта впервые становится **полноценным интерфейсом**, а не
диагностическим инструментом. Соответственно поднимаются требования к
визуализации (фильтры, drill-down, time-slider, compare-mode) и встаёт
задача подключить Антураж к карте — превратить статичный артефакт в
диалоговый.

**Следующая фаза:** доработать визуализацию как первоклассный интерфейс,
прикрутить Антураж к карте для интерактивного уточнения смыслов, и провести
методологический эксперимент по чтению карты без доступа к исходникам.

---

## 2026-06-04 — FVSC как сервис: FastAPI + квантовый retrieval

**Поворот:** От batch-скриптов к standalone HTTP-сервису. FVSC становится
"ядром" — к нему можно подключить любой источник и любой интерфейс.

### Что произошло

**1. FastAPI-обёртка.** 11 эндпоинтов: CRUD пространств, ingest (plain/MD),
per-concept query (contains, contained-in, facets, polysemy, report),
similarity, cross-space compare, квантовый retrieval.
Multi-tenancy через именованные пространства. Auto-save при N ingest-ах.
ThesaurusPrior грузится один раз при старте и шарится между пространствами.

**2. Квантовый retrieval через Tr(ρ_query · ρ_chunk).** Не weighted component
matching, а настоящая операция из density matrix formalism. Запрос (свободный
текст) → ρ_query из basis vectors. Каждый чанк → ρ_chunk из компонентов
с source_text == chunk_id. Score = Tr(ρ_q · ρ_c). На whitepaper (740 чанков,
3902 концепта): 46-314мс/запрос, scores 0.01-0.15. Находит контент ПО ТЕМЕ,
а не по вхождению слов.

**3. Markdown format adapter на mistune AST.** Вместо regex-стриппинга
(уничтожавшего структуру) — полноценный парсинг Markdown в AST. Таблицы
конвертируются в структурированные предложения, заголовки дают контекст
последующим параграфам, списки сохраняют вложенность. +32% концептов
на whitepaper, ноль табличного мусора в retrieval.

**4. BGE удалён из кодовой базы и документации.** Никогда не использовался
в коде — был только задокументирован как запрещённый паттерн (ранг-1
коллапс ρ). Теперь удалён полностью.

**5. Whitepaper дополнен.** Garg & Ramakrishnan (2019) — density matrix
word embeddings, ближайшая existing работа. Busemeyer & Bruza (2012) —
фундаментальная книга quantum cognition.

### Архитектурный итог

```
Любой источник (Obsidian, TG, web, API)
  → POST /spaces/{name}/ingest
  → text_to_semantic_input → SemanticSpace.load_from_semantic_input
  → recursive_deepen
  → GET /spaces/{name}/retrieve (Tr(ρ·ρ))
  → GET /compare (cross-space alignment)
```

Сервис работает. 11/11 тестов проходят. Код в `service/` — чистый слой
поверх `core/`, без изменений в существующем API.

### Обсуждённые направления
- Гибридная архитектура (матрицы плотности + трансформеры) для AGI-подобных систем
- FVSC как семантическая память в такой архитектуре
- Obsidian-плагин как следующий practical шаг
- Самоприменение: FVSC retrieval для поиска по документации FVSC
