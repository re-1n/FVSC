# Актуальные вопросы, пути решения

## Решено

|№|Вопрос|Версия|
|---|---|---|
|—|"Я" как контейнер: [self] routing (identity/relation/action)|v0.3|
|—|Отрицание в модальных оболочках (neg-raising vs non-neg-raising)|v0.4|
|—|Каскадное обнуление модальности (MODALITY_FLOOR=0.05)|v0.4|
|—|Визуализация: рекурсивная навигация внутрь понятий|v0.4|
|—|Bootstrapping: тезаурусный слой (ConceptNet 132K + RuWordNet) с modality=0.3|v0.3|
|—|F1: Слоистая модель интерпретации (interpretation_layer, defeasible, extraction_confidence)|v0.5|
|—|F2: Персональный прогрев — PCA по контекстам при >50 суждениях, замещает spaCy seed|v0.5|
|—|F3: Степенной decay + консолидация + архивация в density_core.py|v0.5|
|—|T6: Сравнение карт двух пользователей (compare_maps)|v0.5|
|—|T10: Интерактивный канал обратной связи (FeedbackEngine)|v0.5|
|—|Cumulative modality corruption в `_handle_copula` (tree_extractor.py)|v0.5.1|
|—|`report_self()` crash при all-archived components (density_core.py)|v0.5.1|
|—|Dedup-ключ mismatch в feedback.py (вопросы повторялись)|v0.5.1|
|—|Multi-word маркеры мертвы в context_classifier.py (bigram scan)|v0.5.1|
|—|XSS в onclick + `</script>` injection в interactive_map.py|v0.5.1|
|—|Silent exception swallowing в antourage_server.py|v0.5.1|

---

## Фундаментальные вопросы

### F1. Слоистая модель интерпретационной уверенности

**Проблема:** Стенографический принцип ("записываем только эксплицитное") уже нарушен — context_classifier совершает инференции (обобщённо-личное "ты", классификация GENERIC по аспекту). Чистый L0 невозможен.

**Решение:** Три явных слоя интерпретации:
- **L0** — прямое извлечение из синтаксиса (текущий пайплайн)
- **L1** — инференции по лингвистическим и "общечеловеческим" правилам, всегда `defeasible=True`
- **L2** — синергия с Антуражем (LLM), всегда `defeasible=True`, `source="antourage"`

**Архитектура:** Добавить в Judgment: `interpretation_layer`, `inference_chain`, `defeasible`. Density matrix считать отдельно по слоям.

**Теоретическая опора:** Grice (1975) said/implicated; Default Logic (Reiter 1980); AGM belief revision (Alchourrón et al. 1985); Kahneman dual-process theory.

**Риск:** L1 ("общечеловеческие принципы") культурно нагружен. Nisbett & Wilson (1977): люди систематически ошибаются в интроспекции.

---

### F2. Проблема заземления (grounding) — **Решено (v0.5)**

**Проблема:** Базовые вектора из spaCy `ru_core_news_md` = коллективные, не персональные.

**Решение (реализовано):** `get_term_vector()` теперь трёхуровневый: персональный PCA (при >50 компонентах) → spaCy seed → детерминированный хеш. Метод `_personal_basis()` строит первую главную компоненту по SVD накопленных контекстов.

**Перспектива (не реализовано):** Гиперболическое вложение (Nickel & Kiela 2017) — v2+.

---

### F3. Забывание со следами (степенной decay)

**Проблема:** `recency = exp(-λ·Δt)` описан в whitepaper, но НЕ реализован в коде. Экспоненциальный decay слишком агрессивен — убьёт фундаментальные убеждения. Без decay карта бесконечно растёт.

**Решение:**
- Степенной decay: `w(t) = w₀ · (1 + Δt/τ)^{-d}`, d ≈ 0.5 — длинный хвост, ничего не обнуляется
- Архивация: компоненты с `w < ε·max(w)` → архив (provenance сохранён, но не в ρ)
- Консолидация (CLS-inspired): повторное подтверждение суждения усиливает base activation
- Per-user τ: начальное = median(inter-message-interval) × 100

**Теоретическая опора:** ACT-R (Anderson 1993) — степенной, не экспоненциальный закон памяти; MINERVA 2 (Hintzman 1988) — отдельные следы; Complementary Learning Systems (McClelland et al. 1995) — гиппокамп → неокортекс.

**Файл:** `core/density_core.py` — Component, `_recompute_rho()`.

---

### F4. Система как модель, не зеркало

**Проблема:** Тезис "могу объяснить → могу формализовать" ограничен. Dreyfus (1972): embodied knowledge не формализуемо. Polanyi (1966): "знаем больше, чем можем сказать". Текст = тонкий срез смысла.

**Решение:** FVSC записывает как человек *объясняет себя*, не как он устроен. Это карта нарратива, не механизма — и для целей проекта это корректно. Box (1976): "All models are wrong, but some are useful."

**Практика:** Добавить `extraction_confidence` к каждому суждению. Не позиционировать как "зеркало сознания".

**Теоретическая опора:** Marr (1982) три уровня анализа; Fodor (1975) Computational Theory of Mind; Nisbett & Wilson (1977).

---

## Открытые технические вопросы

|№|Вопрос|Статус|Путь решения|
|---|---|---|---|
|T1|PMI / ко-окуренция|Шумный на малых корпусах|Bayesian smoothing, PPMI, минимальный count ≥ 5–8|
|T2|Ошибки парсера (7–12% на чатах)|Признано|Multiple parsers → majority vote; confidence score; Антураж корректирует|
|T3|encode() — тензор глагола (DisCoCat)|Открыто|Глагол = тензор d×k×d. Текущее: линейная суперпозиция v_obj + intensity·v_verb. Цель: мультилинейная тензорная свёртка|
|T4|Recursive deepening — сходимость|Открыто|α=0.7, K=3 работают эмпирически, не обоснованы формально. Аналоги: PageRank, GNN|
|T5|Recursive deepening — transform()|**Решено (v0.5)**|`_relation_transform()`: R_r·ρ·R_r† где R_r — ортогональная матрица от доминантного глагола связи|
|T6|Сравнение карт (два пользователя)|**Решено (v0.5)**|`SemanticSpace.compare_maps()` — divergent/aligned/global_similarity|
|T7|Масштаб (>200 текстов)|Не проверено|Когда стабилизируются грани? Нужна эмпирическая проверка|
|T8|Тензорное произведение пространств|Отложено на v2|H_semantic ⊗ H_relational ⊗ H_contextual|
|T9|Evaluation gap|Критично|Нет annotated evaluation set. Нужна формальная оценка precision/recall извлечения|

---

## Вопросы качества кода (ревью 2026-03-28)

Сессия v0.5.1: исправлены 7 критических/средних багов. Ниже — оставшиеся вопросы, требующие решения.

### C1. Type safety в density_core.py — `rho` возвращает `None`, аннотирован как `np.ndarray`

**Где:** `density_core.py:93, 102, 113, 122` — properties `rho`, `rho_norm`, `rho_deep`, `rho_deep_norm`

**Проблема:** Все четыре property аннотированы `-> np.ndarray`, но возвращают `None` когда нет активных компонентов. Код-ревьюер или mypy не предупредит вызывающий код о необходимости проверки `None`.

**Решение:** Заменить на `-> Optional[np.ndarray]`. Уже есть проверки `if r is None` внутри `rho_norm` и `rho_deep_norm`, но аннотации врут.

**Приоритет:** Средний. Не крашит сейчас (все вызывающие уже проверяют), но создаёт ловушку для нового кода.

---

### C2. `_is_verb` — monkey-patched атрибут вместо поля Concept

**Где:** `density_core.py:494` — `verb_concept._is_verb = True`; читается через `getattr(c, '_is_verb', False)` в нескольких местах

**Проблема:** Атрибут не объявлен в dataclass. Не переживёт сериализацию/десериализацию, невидим для type checker, не появляется в `__repr__`.

**Решение:** Добавить `is_verb: bool = False` в `Concept`. Установить в `materialize_judgment` вместо monkey-patch.

**Стоит ли:** Да, простое исправление. Но нужно убедиться что все `getattr(c, '_is_verb', False)` заменены на `c.is_verb`.

---

### C3. `Judgment.timestamp = 0.0` — ловушка для вызывающего кода

**Где:** `density_core.py:30`

**Проблема:** Default timestamp = 0.0 (Unix epoch, 1970). Если вызывающий код забудет установить timestamp, компонент получит ~56 лет decay и фактически нулевой вес. Сейчас `tree_extractor` не устанавливает timestamp вообще — все Judgment'ы имеют `timestamp=0.0`.

**Решение:** Два варианта:
1. `timestamp: float = field(default_factory=time.time)` — авто-заполнение текущим временем
2. Установить timestamp в `tree_extractor.py` при создании Judgment (более явно)

**Стоит ли:** Да, это реальная проблема. Вариант 1 проще, но вариант 2 корректнее (timestamp сообщения, не момент обработки). Для Telegram-данных timestamp должен быть из поля `date` сообщения.

**Приоритет:** Высокий. Сейчас decay работает некорректно для всех суждений из tree_extractor.

---

### C4. `_queryable_concepts` считает архивированные компоненты

**Где:** `density_core.py:590`

**Проблема:** `len(c.components) >= self.min_components` включает архивированные. Concept с 10 архивированными и 0 активными пройдёт проверку, но `rho_deep` вернёт `None` (ловится следующей проверкой, но лишняя работа).

**Решение:** `sum(1 for c in c.components if not c.archived) >= self.min_components`

**Стоит ли:** Да, тривиальный фикс. Но пока decay не работает (C3), архивации не происходит, и баг латентный.

---

### C5. `_extract_clause_arguments` — упрощённая копия `_handle_clause`

**Где:** `tree_extractor.py:484-536`

**Проблема:** Функция обрабатывает consequent-клаузы условных конструкций, но пропускает: passive voice inversion, discourse pointer filtering, INTERLOCUTOR remapping, GENERIC modality reduction для субъектов. Результат: условные конструкции обрабатываются менее качественно.

**Решение:** Два варианта:
1. Рефакторинг: вынести общую логику из `_handle_clause` в shared helper, использовать и там и тут
2. Принять как допустимое упрощение — условные конструкции и так помечены `condition_role="CONSEQUENT"` с пониженной `modality`

**Стоит ли:** Зависит от того, как много условных конструкций в реальном корпусе. На русских чатах ("если..." , "когда...") — много. Рефакторинг стоит сделать, но не срочно.

**Приоритет:** Низкий/средний.

---

### C6. Thread safety: `_condition_counter` в tree_extractor.py

**Где:** `tree_extractor.py:119-122`

**Проблема:** Глобальный `_condition_counter` инкрементируется без блокировки. При многопоточной обработке (web server) — race condition.

**Решение:** `threading.Lock()` или `itertools.count()` (атомарный в CPython из-за GIL, но не гарантирован).

**Стоит ли:** Пока пайплайн однопоточный — нет. При переходе на web server / parallel processing — да.

**Приоритет:** Низкий (отложить до T7 — масштабное тестирование).

---

### C7. Bare imports во всех файлах кроме `demo_feedback.py`

**Где:** `thesaurus_loader.py:28`, `live_test.py:18-20`, `test_poc.py:11`, `visualize_graph.py:23-26`

**Проблема:** `from density_core import ...` работает только если текущая директория = `core/`. При импорте из другой директории (будущий web server, тесты) — `ModuleNotFoundError`.

**Решение:** Два варианта:
1. Добавить `sys.path.insert(0, ...)` как в `demo_feedback.py`
2. Превратить `core/` в пакет (`__init__.py`) и использовать relative imports

**Стоит ли:** Да, но это рефакторинг, не багфикс. Лучше решить при переходе к Phase 5 (frontend + proper packaging).

**Приоритет:** Низкий.

---

### C8. `von_neumann_entropy` возвращает мусор на ненормированных матрицах

**Где:** `density_core.py:242-248`

**Проблема:** Если передать ненормированную ρ (eigenvalues > 1), `log(eigenvalue)` > 0, и `-sum(λ·log(λ))` станет отрицательной — бессмысленное значение для энтропии.

**Решение:** Добавить guard: `assert abs(np.trace(rho) - 1.0) < 0.01` или нормировать внутри функции.

**Стоит ли:** Все текущие вызовы передают нормированную `rho_norm`. Но функция публичная, легко ошибиться. Простой assert — хорошая защита.

**Приоритет:** Низкий.

---

### C9. `self_concept` не участвует в query/comparison операциях

**Где:** `density_core.py:305` — `self_concept` хранится отдельно от `self.concepts` dict

**Проблема:** `[self]` не попадает в `recursive_deepen`, `_queryable_concepts`, `query_contains`, `query_similarity`, `compare_maps`. Нельзя спросить "что содержит [self]?" или "насколько [self] похож на свободу?".

**Решение:** Решение зависит от архитектурного выбора:
- Если `[self]` — контейнер верхнего уровня: включить в `concepts` dict с особым ключом
- Если `[self]` — отдельная сущность: добавить методы `query_self_contains()`, `query_self_similarity()`

**Стоит ли:** Обсудить. Это архитектурный вопрос, связанный с F4 (модель vs зеркало).

**Приоритет:** Средний.

---

### C10. Дубликаты в словаре text_normalizer.py

**Где:** строки 65/122 (`"ваще"`) и 127/129 (`"ниче"`)

**Проблема:** Одинаковые ключи с одинаковыми значениями. Не ломает работу, но сигнал копипасты.

**Решение:** Удалить дубликаты.

**Стоит ли:** Да, тривиально.

---

### C11. Redundant vector computation в `materialize_judgment`

**Где:** `density_core.py:447-451` (anomaly) vs `480-485` (component creation)

**Проблема:** `encode_judgment_for_subject/object` вызывается дважды — сначала для anomaly scoring, потом для создания компонента. Результат детерминированный, вычисления дублируются.

**Решение:** Сохранить `v_subj`, `v_obj` от первого вызова и переиспользовать.

**Стоит ли:** Да, но эффект на производительность минимальный (encode — это lookup + arith).

**Приоритет:** Низкий.
